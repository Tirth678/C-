// Write a program containing functions which counts the number of positive
// integers in an array.
#include<stdio.h>
int main(){
    int n, arr[n], i;  
    printf("Enter the size of the array: ");  
    scanf("%d", &n);  
    printf("Enter the elements: ");  
    for(i = 0; i < n; i++)  
    {  
        scanf("%d", &arr[i]); 
        for(i=0; i>0; i++){
            printf("the positive elements = %d\n", arr[n]);
        } 
    } 
    
    

    return 0;
}
// i am not able to solve your help would be appreciated.